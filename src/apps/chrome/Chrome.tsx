import Window, { WindowProps } from "../../components/window/Window";

const Chrome = (props: WindowProps) => {
  return <Window {...props} title="Google Chrome"></Window>;
};

export default Chrome;
